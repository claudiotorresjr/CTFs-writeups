<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fluidctf.messenger"
    android:versionCode="14"
    android:versionName="3.2.1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="FluidMsg"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <!-- Main launcher activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Chat activity -->
        <activity
            android:name=".ChatActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Settings activity -->
        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />

        <!-- Profile activity -->
        <activity
            android:name=".ProfileActivity"
            android:exported="false"
            android:parentActivityName=".SettingsActivity" />

        <!-- Admin panel (PRIVATE - not exported) -->
        <activity
            android:name=".AdminPanelActivity"
            android:exported="false"
            android:permission="com.fluidctf.messenger.permission.ADMIN" />

        <!-- Third-party Analytics SDK redirect handler -->
        <activity
            android:name=".sdk.AnalyticsRedirectActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="com.fluidctf.messenger.sdk.ANALYTICS_REDIRECT" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Analytics SDK receiver -->
        <receiver
            android:name=".sdk.AnalyticsReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.fluidctf.messenger.sdk.ANALYTICS_EVENT" />
            </intent-filter>
        </receiver>

        <!-- Push notification service -->
        <service
            android:name=".PushNotificationService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>
